# 2026-04-18

## Carry-Forward from 2026-04-14

### Done last session
- Project renamed: **Soma — Powered by Theo**. Architecture decisions consolidated.
- Backend scaffolded: 24 files, all parses clean. Orchestrator LangGraph, nutrition agent, FastAPI routes, Clerk auth, Qdrant memory, model router (Haiku/Sonnet/Titan via Bedrock).
- **Talos Terraform module** (`modules/talos/`) — revised with split files (data.tf, security_group.tf, main.tf), CP via `modules/aws/ec2`, workers as ASG, NLB optional, kubeconfig in Secrets Manager.
- **humboldt cluster wired** in `platform/humboldt/aws/` — t3.small CP, t3.medium workers ASG (1–5), private subnet + Tailscale-only access, NLB disabled.
- **soma-app Helm chart** (`gitops/charts/soma-app/`) — one chart for all services, KEDA/HPA mutually exclusive, all per-agent app folders built.

### Handed off to today
1. `terraform init && terraform plan` on `platform/humboldt/aws/` — **review plan before touching apply**
2. Fitness, Meal, Coach agents — backend Python services (each own FastAPI app + Dockerfile)
3. Temporal workflow — stub in chat route → real integration (session state load from S3)
4. Tests — orchestrator + nutrition agent
5. IRSA Terraform module (`modules/aws/irsa/`) — OIDC claim + IAM role + app policies combined
6. CI/CD pipelines — build → push to ECR → update image tag in gitops

### Open backlog (not started)
- K8s-Bootstrap repo cleanup — remove `afo-pi-cluster/` dir, replace with `nebulosa/` structure
- Tailscale ACL policy + tags via Terraform (`tag:nebulosa`, `tag:humboldt`)
- Tailscale EC2 subnet router Terraform module (advertises VPC private CIDRs into tailnet)
- Validate AWS → nebulosa via Tailscale
- Phase 3 infra: DynamoDB tables, S3 bucket, Secrets Manager entries (Nutritionix, LangSmith), ECR repos
- Phase 4: actual agent deployments (orchestrator on nebulosa, specialists on humboldt)
- Langfuse deployment on AWS Talos

---

## Concerns / Tech Debt

### nebulosa device config lives in the wrong place
`platform/humboldt/tailscale/devices.tf` defines `nebulosa_devices` (pi-1/2/3 tags) — nebulosa
concerns have no business in a humboldt Terraform root. Should move to `platform/nebulosa/tailscale/`
with its own state. Humboldt tailscale root should only know about humboldt devices (the EC2 subnet
router, humboldt ACL tags, etc.).

---

## Today's Work

## Cluster Bootstrap — Both Clusters

### humboldt (AWS Talos)
- Talos cluster provisioned via Terraform (`platform/humboldt/aws/`)
- 1 control plane (t3.small) + 2 workers (t3.medium ASG), all Ready
- kubeconfig merged into `~/.kube/config` as context `admin@humboldt`
- Gateway API CRDs v1.2.1 applied (server-side): GatewayClass, Gateway, HTTPRoute,
  GRPCRoute, ReferenceGrant

### nebulosa (RPi k3s)
- k3s already running (wiped + reinstalled 9 days ago)
- Kubeconfig was stale (old CA from pre-wipe install). Fixed by fetching fresh kubeconfig
  from pi-1 via SSH, deleting old `admin@nebulosa` entries, re-merging.
  Rule: after any k3s reinstall, always re-fetch kubeconfig from the node.
- Gateway API CRDs v1.2.1 applied (server-side)

### K8s-Bootstrap repo structure established
```
K8s-Bootstrap/
├── humboldt/
│   ├── argocd/            (values.yaml, namespace.yaml)
│   ├── gateway-api/       (kustomization.yaml → upstream v1.5.0)
│   └── envoy-gateway/     (kustomization.yaml → upstream v1.7.2 + gatewayclass.yaml)
└── nebulosa/
    ├── gateway-api/       (kustomization.yaml → upstream v1.5.0)
    └── envoy-gateway/     (kustomization.yaml → upstream v1.7.2 + gatewayclass.yaml + inline resource patch)
```

No base/ directory — each cluster's overlay references upstream directly. Tried base/overlay
with remote URL: kustomize can't resolve remote URLs from a local base, only from root.
Each overlay declares the upstream URL itself.

Gateway API v1.5.0 required by Envoy Gateway v1.7.2 (compatibility matrix confirmed).
Applied to both clusters server-side.

### Envoy Gateway — bundled CRD conflict (expected, harmless)

Envoy Gateway v1.7.2's `install.yaml` bundles older Gateway API CRDs. The `safe-upgrades`
ValidatingAdmissionPolicy (installed by Gateway API v1.5.0 standard channel) blocks these:

```
Error: customresourcedefinitions.apiextensions.k8s.io "gatewayclasses.gateway.networking.k8s.io"
  is forbidden: ValidatingAdmissionPolicy 'safe-upgrades.gateway.networking.k8s.io' denied request:
  Installing experimental CRDs on top of standard channel CRDs is prohibited by default.
```

This is correct behavior — our v1.5.0 CRDs are newer and should not be downgraded. The Envoy
Gateway controller itself deploys fine. All Envoy-specific CRDs (BackendTrafficPolicy,
SecurityPolicy, etc.) apply without issue.

### GatewayClass — must be created explicitly, named per cluster

Envoy Gateway's `install.yaml` does NOT include a GatewayClass resource — only the CRD definition.
The GatewayClass instance must be created separately as `gatewayclass.yaml` in each overlay.
Named per cluster (`humboldt-envoy-gw`, `nebulosa-envoy-gw`) — not the generic `eg` default.
Naming convention: immediately obvious which cluster owns it.

Result: both GatewayClasses `Accepted: True`.

### Two Gateways per cluster — internal and external

Added `gateway-internal.yaml` and `gateway-external.yaml` to each cluster's envoy-gateway overlay.

- `humboldt-internal` — admin tools (ArgoCD, Grafana). Tailscale-only access.
- `humboldt-external` — public services via Cloudflare tunnel.

Both are defined in `envoy-gateway/` because they're tightly coupled to the GatewayClass and
EnvoyProxy config that lives there. Cleaner than a separate `gateway/` directory.

### ArgoCD installed on humboldt

Helm chart via kustomize (`helmCharts:` block), applied with:
```
kustomize build --enable-helm | kubectl apply --server-side --context admin@humboldt -f -
```

Note: `kubectl apply -k` bundles an older kustomize that doesn't support `helmCharts:`.
Always use standalone `kustomize` binary for helm-based kustomizations.

ArgoCD config:
- `server.insecure: true` — Cloudflare tunnel terminates TLS at edge
- `global.domain: argocd.cloudhaven.work`
- Single replicas for all components (right-sized for t3 workers)
- Dex enabled — future Authentik OIDC integration

### HTTPRoute + GRPCRoute for ArgoCD

ArgoCD serves both browser traffic (HTTP/1.1) and CLI traffic (gRPC/h2c) on port 80.
Required two route objects, both attached to `humboldt-internal`:

- `HTTPRoute argocd` — web UI
- `GRPCRoute argocd-grpc` — argocd CLI

`server.service.servicePortHttpAppProtocol: kubernetes.io/h2c` set in values — tells Envoy
to use HTTP/2 cleartext when connecting to the backend pod.

### Tailscale Kubernetes Operator — decision log

Envoy Gateway creates LoadBalancer services for each Gateway. On raw EC2 Talos (no EKS),
there's no cloud controller to fulfill them → `<pending>` external IP forever.

Alternatives considered:
- NodePort — works (NodePorts are open), Tailscale gets you to node IP. Ugly port numbers.
- AWS Load Balancer Controller — ~$16/mo per NLB, adds IRSA complexity, overkill for admin tools.
- MetalLB — Layer 2 doesn't work reliably on AWS VPC. BGP needs router config. Not worth it.
- **Tailscale Kubernetes Operator** ← chosen. Assigns real Tailscale IPs to LoadBalancer
  services. humboldt-internal gets a stable Tailscale hostname. No port numbers, no NLB cost,
  fits the mesh we already have. cloudflared (external gateway) stays ClusterIP — it connects
  inbound from inside the cluster anyway.

EnvoyProxy CRDs scaffolded (`envoyproxy-internal.yaml`, `envoyproxy-external.yaml`) but not
applied yet — pending Tailscale operator install.

### Tailscale operator — Terraform scaffolded

`platform/humboldt/tailscale/operator-oauth.tf`:
- `tailscale_oauth_client` scoped to `tag:k8s-humboldt` with `devices` write scope
- Writes client ID + secret to SSM at `/tailscale/humboldt/operator/client-id` and
  `/tailscale/humboldt/operator/client-secret`
- `tag:k8s-humboldt` added to `tagOwners` in `acl.tf`

### New module: `modules/tailscale/device-tags`

Reusable module — takes `devices = map(hostname → tags)`, handles data lookup and tag assignment.
Called from each cluster's tailscale root instead of duplicating the locals + resources.

### nebulosa Tailscale devices moved to correct root

Was: `platform/humboldt/tailscale/devices.tf` (wrong — nebulosa concerns in humboldt root)
Now: `platform/nebulosa/tailscale/devices.tf` using the new module

Migration steps before applying:
1. `terraform state rm` the nebulosa device resources from humboldt state
2. Fill in device IDs in `platform/nebulosa/tailscale/devices.tf` import blocks
   (`tailscale status --json | jq '.Peer[] | {(.HostName): .ID}'`)
3. `terraform init && terraform apply` on nebulosa tailscale

### Tailscale Terraform — both states applied successfully

- `platform/humboldt/tailscale/` — ACL updated (`tag:k8s-humboldt` added), OAuth client created,
  credentials in SSM at `/restricted/tailscale/operator/client-id` and `client-secret`.
  Nebulosa device tags removed cleanly via `removed {}` blocks (no destroy in Tailscale).
- `platform/nebulosa/tailscale/` — new state, nebulosa devices imported via import blocks,
  now owns pi-1/2/3 device tags going forward.
- `modules/tailscale/device-tags/` — reusable module, called by both roots.

### ESO scaffolded for humboldt

`K8s-Bootstrap/humboldt/external-secrets/` — ESO operator install only (helm via kustomization).
No ClusterSecretStore — each namespace defines its own SecretStore scoped to it:
  `tailscale/secretstore.yaml`, `argocd/secretstore.yaml`, etc.
ClusterSecretStore would give cluster-wide SSM access — breaks separation of concerns.

ServiceAccount `external-secrets-sa` created by helm chart with IRSA annotation placeholder.
Blocked on IRSA module (not yet built).

### Cloudflare Terraform scaffolded
`platform/humboldt/cloudflare/` — calls tunnel module + dns module, ingress rules in
own `ingress.tf`. Needs SSM params before apply:
- `/cloudflare/api-token`
- `/cloudflare/account-id`

### New modules
- `modules/cloudflare/dns/` — reusable DNS record module (CNAME, A, TXT, etc.)

---

## Terraform Apply — humboldt Talos Cluster (Debugging)

First apply of `platform/humboldt/aws/` hit three separate blockers:

### Bug 1 — ASG tags with `/` rejected by AWS

`k8s.io/cluster-autoscaler/enabled` and `k8s.io/cluster-autoscaler/<cluster>` use `/` which AWS rejects
in propagated ASG tags (`propagate_at_launch = true`). The cluster autoscaler reads ASG tags directly
via the ASG API — it does not need them on instances. Fix: set both to `propagate_at_launch = false`
in `modules/talos/main.tf`.

### Bug 2 — Talos API SG only allowed Tailscale CGNAT range; subnet router SNATs

`talos_api_allowed_cidr = "100.64.0.0/10"` was correct in theory — Tailscale IPs should come from
that range. But Tailscale subnet routers perform SNAT when forwarding traffic into the VPC. The Talos
node sees source IP as the subnet router's VPC IP (`10.0.101.25`), not the originating Tailscale IP.
SG silently dropped all traffic → `i/o timeout` on port 50000.

Fix: changed `talos_api_allowed_cidr` (string) to `talos_api_allowed_cidrs` (list) in the module.
Passed `["100.64.0.0/10", "10.0.0.0/16"]` from `cluster.tf` — Tailscale CGNAT for direct future
access, VPC CIDR for subnet-router-proxied access today.

### Bug 3 — `tailscale up --accept-routes` silently ignored due to version mismatch

Mac had Tailscale app (tailscaled 1.96.5) already installed. Brew installed CLI 1.96.4. Running
`sudo tailscale up --accept-routes` via the brew binary against the app's tailscaled produced a
version mismatch warning and did NOT apply the setting — `tailscale status` showed:
`Some peers are advertising routes but --accept-routes is false`.

Fix: use the app's own binary directly:
```
sudo /Applications/Tailscale.app/Contents/MacOS/Tailscale set --accept-routes
```

Rule for future: when Tailscale app is installed on macOS, always use
`/Applications/Tailscale.app/Contents/MacOS/Tailscale` for CLI commands, not a separately
installed brew binary. Version mismatch = silent failures.

### TODO (deferred)
- Read-only kubeconfig for humboldt: ServiceAccount "readonly" in kube-system → ClusterRoleBinding
  to built-in `view` ClusterRole → long-lived token Secret → kubeconfig with `token:` field.
  Built-in `view` role: get/list/watch on most resources, no secrets access.

### Outcome

humboldt cluster is up. `kubectl get nodes` works from Mac via Tailscale subnet router.
kubeconfig merged into `~/.kube/config` as context `humboldt`.

### Additional bugs fixed during apply

**Bug 4 — `talos_cluster_health` data source blocks every future apply**
The `data "talos_cluster_health"` resource runs on every `terraform apply` and waits for all
k8s nodes to report healthy (including worker ASG nodes). Workers take several minutes after
the ASG spins them up, making every apply hang for 5–10+ minutes. Removed from the module —
cluster health is better verified with `kubectl get nodes` after apply.

**Bug 5 — kubeconfig double base64 in Secrets Manager**
`talos_cluster_kubeconfig.kubernetes_client_configuration.ca_certificate` (and client_certificate,
client_key) already returns base64-encoded PEM. The module wrapped them in `base64encode()` again,
producing double base64. Kubeconfig was unreadable (`unable to parse bytes as PEM block`).
Fixed: removed `base64encode()` from the `aws_secretsmanager_secret_version` resource in
`modules/talos/main.tf`. The Python script used to build the initial kubeconfig decoded once
as a workaround.

---

## TODO — Next Session

### Priority 1 — OIDC + IRSA (unblocks ESO, Tailscale operator, all workload identity)

**humboldt (Talos/EC2)**
- Talos already exposes an OIDC discovery endpoint via the EC2 instance IAM role
- Confirm endpoint URL, create `aws_iam_openid_connect_provider` for humboldt
- Scaffold `modules/aws/irsa/` — reusable module: OIDC provider ref + IAM role + policy attachment
- Wire ESO ServiceAccount in `K8s-Bootstrap/humboldt/external-secrets/values.yaml`

**nebulosa (k3s/RPi)**
- Existing pattern: `platform/pi-cluster/aws/iam/oidc-providers/pi-oidc.tf`
  uses `https://oidc.cloudhaven.work` (Cloudflare alias → k3s OIDC endpoint)
- New alias: `oidc-nebulosa.cloudhaven.work` — Cloudflare DNS → nebulosa k3s OIDC
- Create `aws_iam_openid_connect_provider` for nebulosa in new `platform/nebulosa/aws/`
- Scaffold `platform/nebulosa/aws/providers.tf` (new state)

### Priority 2 — Tailscale Operator (unblocks EnvoyProxy + Gateway addresses)
- Scaffold `K8s-Bootstrap/humboldt/tailscale-operator/` — operator helm install + ExternalSecret
  pulling from `/restricted/tailscale/operator/client-id` and `client-secret`
- ExternalSecret needs ESO running + SecretStore in `tailscale` namespace
- After operator is up: apply EnvoyProxy resources → `humboldt-internal` gets Tailscale IP

### Priority 3 — ESO SecretStore per namespace
- `K8s-Bootstrap/humboldt/tailscale-operator/secretstore.yaml` — scoped to `tailscale` ns
- `K8s-Bootstrap/humboldt/argocd/secretstore.yaml` — scoped to `argocd` ns (future use)
- Pattern: `SecretStore` not `ClusterSecretStore` — each namespace owns its own

### Priority 4 — Cloudflare Terraform apply
- Populate SSM: `/cloudflare/api-token`, `/cloudflare/account-id`
- `terraform apply` on `platform/humboldt/cloudflare/`

### Backlog
- Read-only kubeconfig for humboldt (ServiceAccount + view ClusterRole + long-lived token)
- `removed.tf` in humboldt tailscale — delete after confirming nebulosa state is clean
- `platform/nebulosa/tailscale/devices.tf` import blocks — delete after first apply
- K8s-Bootstrap: remove old `afo-pi-cluster/` directory
- Cloudflare DNS alias `oidc-nebulosa.cloudhaven.work` via Terraform
- Phase 3 infra: DynamoDB, S3, ECR repos, Secrets Manager entries (Nutritionix, LangSmith)
- Phase 4: agent deployments
- Cloudflare provider v5 migration — `cloudflare_tunnel` → `cloudflare_zero_trust_tunnel_cloudflared`,
  `cloudflare_tunnel_config` → `cloudflare_zero_trust_tunnel_cloudflared_config`. Schema changed
  significantly (no `secret` attr, different `config` block). Pinned to `~> 4.0` for now.
  Deprecation warning only — removal in v6, not v5.
- cert-manager on humboldt-external — Let's Encrypt ClusterIssuer for public-facing services,
  drops `no_tls_verify` on OIDC tunnel ingress once TLS is terminated at Envoy Gateway.

---

---

## OIDC + IRSA — Completed This Session

### OIDC discovery endpoint live on humboldt

Talos kube-apiserver patched with two extra args via `modules/talos/data.tf` `oidc_patch`:
- `service-account-issuer = https://oidc-humboldt.cloudhaven.work`
- `service-account-jwks-uri = https://oidc-humboldt.cloudhaven.work/openid/v1/jwks`
- `anonymous-auth = "true"` — Talos disables this by default (k3s allows it by default)

Without `anonymous-auth`, the endpoint returned 401. With it + the wrong ClusterRole, 403.

**ClusterRoleBinding fix:** `system:public-info-viewer` does NOT cover `/.well-known/openid-configuration`
or `/openid/v1/jwks`. The correct built-in role is `system:service-account-issuer-discovery`.
Updated `platform/humboldt/cloudflare/rbac.tf` — applied via `terraform apply`.

**jwks_uri fix:** kube-apiserver defaulted `jwks_uri` in the discovery doc to the internal node IP
(`https://10.0.101.165:6443/openid/v1/jwks`). AWS can't reach that. Adding `service-account-jwks-uri`
overrides it to the public Cloudflare tunnel URL.

Confirmed working:
```
curl https://oidc-humboldt.cloudhaven.work/.well-known/openid-configuration
# → 200, jwks_uri: https://oidc-humboldt.cloudhaven.work/openid/v1/jwks
```

**502 debugging note:** cloudflared was getting `dial tcp 10.96.0.1:443: connect: connection refused`
despite the apiserver being up. busybox pod in the same namespace could reach port 443 fine.
Root cause was the ClusterRoleBinding — not a networking issue. The 502 resolved once the
RBAC was correct and cloudflared restarted with the updated tunnel config.

### AWS OIDC provider registered

`platform/humboldt/aws/oidc.tf` — `aws_iam_openid_connect_provider.humboldt`:
- `url = https://oidc-humboldt.cloudhaven.work`
- thumbprint fetched via `data.tls_certificate.humboldt_oidc` (in `data.tf`)
- `client_id_list = ["sts.amazonaws.com"]`
- `tls` provider (`~> 4.0`) added to `providers.tf`

### New module: `modules/aws/iam/app_role`

Reusable IRSA role module. Creates an IAM role with OIDC trust policy scoped to a single
Kubernetes service account. Inputs:
- `oidc_provider_arn`, `oidc_provider_url` (no `https://`)
- `namespace`, `service_account_name`
- `role_name`
- `policy_arns` — list of managed policy ARNs (attached as separate `aws_iam_role_policy_attachment`)
- `inline_policy_json` — optional, attached as `aws_iam_role_policy` if provided

Trust policy uses `StringEquals` on both `:sub` (service account) and `:aud` (`sts.amazonaws.com`).
ESO itself does NOT get this role — it's for the application whose secrets ESO is fetching.
Each app (Tailscale operator, future workloads) gets its own scoped role via this module.

---

## TODO — Next Session

### Priority 1 — Tailscale Operator install
- `K8s-Bootstrap/humboldt/tailscale-operator/` — operator helm install (kustomize helmCharts)
- `ExternalSecret` pulling `client-id` + `client-secret` from SSM `/restricted/tailscale/operator/`
- `SecretStore` scoped to `tailscale` namespace — ESO must be running first
- After operator up: apply EnvoyProxy resources → `humboldt-internal` gets Tailscale IP

### Priority 2 — ESO apply + IRSA role
- `terraform init && terraform apply` on `platform/humboldt/aws/` (OIDC provider + any new roles)
- Apply ESO helm chart via kustomize
- Create `app_role` for ESO's service account scoped to SSM read
- Wire `serviceAccount.annotations` in ESO values with role ARN

### Priority 3 — nebulosa OIDC
- `oidc-nebulosa.cloudhaven.work` Cloudflare DNS alias
- k3s `service-account-issuer` + `service-account-jwks-uri` flags
- `aws_iam_openid_connect_provider` for nebulosa in `platform/nebulosa/aws/`

### Backlog
- Tunnel module file renames: `variable.tf` → `variables.tf`, `output.tf` → `outputs.tf`,
  `provider.tf` → `versions.tf` (still pending from earlier session)
- Read-only kubeconfig for humboldt
- `removed.tf` in humboldt tailscale — delete after confirming nebulosa state is clean
- `platform/nebulosa/tailscale/devices.tf` import blocks — delete after first apply
- Cloudflare provider v5 migration (pinned to `~> 4.0`, deprecation only, no urgency)
- cert-manager on humboldt-external for Let's Encrypt
- K8s-Bootstrap: remove old `afo-pi-cluster/` directory
- Phase 3 infra: DynamoDB, S3, ECR repos, Secrets Manager entries

---

---

## Tailscale Operator + EnvoyProxy + Gateways — Completed This Session

### ESO deployed (v2.x)

Afolabi bumped the ESO chart version to 2.1.0 (was 0.14.4). API version changed from
`external-secrets.io/v1beta1` → `external-secrets.io/v1` — all SecretStore and ExternalSecret
manifests updated to match. ESO itself does NOT need an IRSA role — each `SecretStore` references
its own dedicated SA via `auth.jwt.serviceAccountRef`, so ESO uses that SA's projected token to
call AWS, not its own.

### Tailscale operator deployed

Afolabi applied `K8s-Bootstrap/humboldt/tailscale-operator/` — helm chart v1.80.0 via kustomize.

**Issues hit and fixed:**

1. **`operator-oauth` secret missing** — ESO ExternalSecret blocked because SecretStore wasn't ready.
   Afolabi caught this from `kubectl describe externalsecret`. Root cause: SecretStore referenced a SA
   that didn't exist. Fixed by aligning the SA name with what the helm chart actually creates
   (`operator`, not `tailscale-operator`).

2. **tag:k8s-operator invalid** — operator defaulted to `tag:k8s-operator` for its tsnet device.
   Afolabi correctly pointed out we already have `tag:k8s-humboldt` — no need to add a new tag.
   Fixed via `operatorConfig.defaultTags: ["tag:k8s-humboldt"]` in values.yaml.

3. **PROXY_TAGS still `tag:k8s`** — service proxy devices use a separate env var (`PROXY_TAGS`),
   different from the operator's own device. `proxyConfig.defaultTags` controls it.
   Fixed: `proxyConfig.defaultTags: "tag:k8s-humboldt"` (must be a string, not a YAML list).

4. **Helm SA name hardcoded** — Afolabi pushed back on the separate `serviceaccount.yaml` approach
   and asked to understand the helm chart first. SA is hardcoded as `operator` with no fullname
   override (upstream issue: github.com/tailscale/tailscale/issues/18232). Worked around via
   kustomize `patches:` block to annotate the `operator` SA with the IRSA ARN.

5. **PodSecurity `baseline` blocked proxy pod** — Tailscale proxy containers run privileged.
   Afolabi ran `kubectl describe statefulset` to surface the error. Fixed: added
   `pod-security.kubernetes.io/enforce: privileged` label to the `tailscale` namespace.

### EnvoyProxy — both gateways PROGRAMMED: True

Afolabi applied EnvoyProxy resources and worked through gateway reconciliation.

`humboldt-external` → `ClusterIP` — cloudflared connects inbound, no external IP needed.
`humboldt-internal` → `LoadBalancer` + `loadBalancerClass: tailscale` + annotation `tailscale.com/hostname: humboldt-internal`.

Key learnings:
- Afolabi correctly challenged the two-GatewayClass approach — `infrastructure.parametersRef` on
  the Gateway IS supported in EG v1.7.2. One GatewayClass is the right model. Reverted.
- Existing services must be deleted and recreated when EnvoyProxy changes — Envoy Gateway won't
  mutate a service it already owns.
- Auto-generated service name exceeds Tailscale's 63-char hostname limit — fixed via
  `envoyService.annotations: tailscale.com/hostname: humboldt-internal` in EnvoyProxy spec.
- `loadBalancerClass: tailscale` must be set on the service or the operator ignores it (by design).

Result:
```
humboldt-internal   humboldt-envoy-gw   100.78.197.38    True
humboldt-external   humboldt-envoy-gw   10.102.199.239   True
```

Tailscale DNS: `humboldt-internal.nebulosa-humboldt.ts.net`

### New modules / IAM

- `modules/aws/iam/app_role` — Afolabi asked to reuse the existing `modules/aws/iam/role` instead
  of duplicating IAM logic. `app_role` now wraps `role`, builds OIDC trust policy via
  `aws_iam_policy_document` in `data.tf`, derives OIDC URL from `cluster` + `domain` vars.
- `modules/cloudflare/tunnel` — Afolabi asked for `additional_labels` support. Default labels now
  use `app.kubernetes.io/*` convention. Selector uses `app.kubernetes.io/instance`.
- `iam_tailscale.tf` — Afolabi chose inline policy over a separate `aws_iam_policy` resource.
  Data sources and policy documents live in `data.tf`, IAM resources in `iam_<workload>.tf`.

---

## TODO — Next Session

### Priority 1 — Split-horizon DNS for internal services
- ArgoCD reachable at `100.78.197.38` over Tailscale but HTTPRoute matches on hostname
  `argocd.cloudhaven.work` — currently requires `/etc/hosts` entry on Mac.
- Long-term: Tailscale nameserver override in ACL so `argocd.cloudhaven.work` resolves to
  `100.78.197.38` inside the tailnet, Cloudflare tunnel outside.
- Short-term workaround: add `100.78.197.38 argocd.cloudhaven.work` to `/etc/hosts`.

### Priority 2 — nebulosa OIDC
- `oidc-nebulosa.cloudhaven.work` Cloudflare DNS + k3s `service-account-issuer` flag
- `aws_iam_openid_connect_provider` for nebulosa in `platform/nebulosa/aws/`

### Priority 3 — ArgoCD managing both clusters
- Register nebulosa as downstream cluster in ArgoCD (running on humboldt)
- Bootstrap GitOps app-of-apps

### Backlog
- Tunnel module file renames: `variable.tf` → `variables.tf`, `output.tf` → `outputs.tf`,
  `provider.tf` → `versions.tf`
- Read-only kubeconfig for humboldt
- `removed.tf` in humboldt tailscale — delete after confirming nebulosa state is clean
- `platform/nebulosa/tailscale/devices.tf` import blocks — delete after first apply
- Cloudflare provider v5 migration (pinned to `~> 4.0`, no urgency)
- cert-manager on humboldt-external for Let's Encrypt
- K8s-Bootstrap: remove old `afo-pi-cluster/` directory
- Phase 3 infra: DynamoDB, S3, ECR repos, Secrets Manager entries

---

## Blog Posts (to write)

**Post 1 — Bootstrapping bare-metal + cloud clusters without a managed LB**
How we run Gateway API + Envoy Gateway across a home RPi k3s cluster and raw EC2 Talos
without EKS or any cloud load balancer controller. Two Gateways (internal/external), Tailscale
operator fulfilling LoadBalancer services with real tailnet IPs, cloudflared on ClusterIP for
the public path. What the architecture looks like and why each piece is where it is.

**Post 2 — Tailscale Kubernetes Operator: what it actually does**
Written from first principles for someone whose employer is evaluating it. How the operator
creates tailnet devices for LoadBalancer services, what the OAuth scopes and ACL tags mean,
how you'd use it in a multi-cluster mesh, and what the IRSA/secret delivery story looks like
in a GitOps setup with ESO.
